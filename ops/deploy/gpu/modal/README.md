# GPU Worker on Modal

This template is for a Modal-managed inference worker wrapping
`converge-llm-server` or a future direct Python/Rust bridge.

The current template is intentionally minimal and should be treated as a
starting point for your own image, mounted model weights, and secrets.
